# Override layer. The canonical data lives in the standalone config files
# (thresholds.yml, model_portfolios.yml, tax_assumptions.yml, account_menus.yml), which are
# loaded first; values set here override them, and --config / CLI flags override these in turn.
defaults:
  as_of_date: "2026-06-01"
  target_profile: balanced
